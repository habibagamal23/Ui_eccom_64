import 'package:ecommerce_64/navigation/screndScreen.dart';
import 'package:flutter/material.dart';

class FirstScreen extends StatelessWidget {
  const FirstScreen({super.key});

  @override
  Widget build(BuildContext context) {
    var name = "habiba";
    return Scaffold(
      body: Center(
          child: Column(children: [
        Container(
          height: 100,
          width: double.infinity,
          child: Column(children: [
            Image.network(
                height: 50,
                "https://images.pexels.com/photos/1640777/pexels-photo-1640777.jpeg?auto=compress&cs=tinysrgb&w=600"),
            SizedBox(height: 20),
            Text("pizza")
          ]),
        ),
        SizedBox(height: 20),
        InkWell(
          onTap: () {
            Navigator.push(context,
                MaterialPageRoute(builder:
                    (context) => DeatilsScreen(
                      Name: "burrger",
                    image:"https://images.pexels.com/photos/1640777/pexels-photo-1640777.jpeg?auto=compress&cs=tinysrgb&w=600"),
                    ));
          },
          child: Container(
            height: 100,
            width: double.infinity,
            child: Column(children: [
              Image.network(
                  height: 50,
                  "https://images.pexels.com/photos/1640777/pexels-photo-1640777.jpeg?auto=compress&cs=tinysrgb&w=600"),
              SizedBox(height: 20),
              Text("burrger")
            ]),
          ),
        )
      ])),
    );
  }
}

class DeatilsScreen extends StatelessWidget {
  var Name;
  var image;

  DeatilsScreen({super.key, required this.Name, required this.image});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
      child: Column(
        children: [
          Image.network(height: 50, image),
          SizedBox(height: 20),
          Text(Name)
        ],
      ),
    ));
  }
}
